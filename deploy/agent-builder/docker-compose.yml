version: '3.9'
services:
  agent-builder:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ../../:/app
    command: bash -lc "pip install -e . fastapi uvicorn && PYTHONPATH=apps/agent-builder uvicorn main:app --host 0.0.0.0 --port 8080"
    ports:
      - "8080:8080"
